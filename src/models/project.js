/*

const schema = new mongoose.Schema(
    {name: 'string',
    details: 'string',
    state: 'string',
    keywords: 'string'
    })

const project = mongoose.model('Project', schema)

export default project*/